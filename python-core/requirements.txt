# Sidecar runtime
fastapi>=0.115
uvicorn>=0.32
pydantic>=2.9
httpx>=0.27        # provider HTTP calls (Ollama / LM Studio / Gemini)
pillow>=10.0

# --- engine parsers (uncomment as you implement each engine) ---
UnityPy>=1.24.0      # Unity .assets / bundles
# anthropic>=0.40      # only if you ever move LLM calls server-side
